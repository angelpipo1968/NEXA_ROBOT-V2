import MediaGenerator from '@/features/media/MediaGenerator';

export default function MediaGeneratorPage() {
    return (
        <section className="flex flex-col h-full overflow-hidden relative">
            <MediaGenerator />
        </section>
    );
}
