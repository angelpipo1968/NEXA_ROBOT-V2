'use client';

import { Sidebar } from '@/components/layout/Sidebar';
import { useState } from 'react';

export default function SidebarWrapper() {
    return <Sidebar onOpenSettings={() => { }} />;
}
